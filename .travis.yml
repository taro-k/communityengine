language: ruby

bundler_args: --binstubs

sudo: false
cache: bundler

rvm:
  - 2.0.0
  - 2.1.9
  - 2.2.5
  - 2.3.1
env: "RAILS_ENV='test' RAILS_VERSION=4.2.0"

script: "bundle exec rake db:drop db:create db:migrate test"
